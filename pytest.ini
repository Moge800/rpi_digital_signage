# pytest設定

[tool.pytest.ini_options]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 詳細な出力
addopts = -v --strict-markers --tb=short

# マーカー定義
markers =
    slow: 実行に時間がかかるテスト
    integration: 統合テスト
    unit: 単体テスト
